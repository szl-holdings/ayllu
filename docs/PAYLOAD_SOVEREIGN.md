# PAYLOAD — sovereign occupy (no vendor key required)

Canonical: this repo. Hub: SZLHOLDINGS/ayllu (MEASURED Running, backend SOFTWARE).
Speech order: local OpenAI-compatible runtime (empty key) → optional XAI → SOFTWARE remit.

Run `python scripts/ayllu_occupy.py` then `python scripts/ayllu_sovereign.py`.
Λ = Conjecture 1. Handles only. UNSIGNED receipts.
