# Psyche — neural-symbolic architecture

Operational organ. Stdlib only. Joules stay `null`. Λ = Conjecture 1.

## Layers

1. **Yuyay** (`ayllu.psyche.neural`) — sparse bipolar encode, Hebb+Oja with
   Frobenius bound, BCM sliding threshold, reverse-Hebb replay, dual recall
   (classical Hopfield + softmax). Honesty-weighted write gain: UNAVAILABLE
   cannot write synapses.
2. **Tinku** (`ayllu.psyche.morphisms`) — fail-closed morphisms. Honesty is a
   lattice; composition is the meet. BLOCKED is absorbing. Associative.
3. **Khipu graph** (`ayllu.psyche.graph`) — typed hypergraph of 11 seats, 5
   organs, and engrams. Incidence is SOFTWARE topology.
4. **Lock** (`ayllu.psyche.lock`) — Human Lock + restraint ladder + autonomy
   gate. Attestation binds; Λ floor is advisory.
5. **Engine** (`ayllu.psyche.engine`) — one pulse: cue → recall → seat →
   optional imprint → UNSIGNED-honest SHA3 receipt.

Seats are remitted arrows on one routed backend, not eleven trained models.

Capacity α vs 0.138 is MODELED, not a proof. Kleiber watts are analog.
Energy in Hopfield is a Lyapunov number, not a joule.

## Endpoints

- `GET /psyche`
- `GET /api/v1/psyche/health`
- `GET /api/v1/psyche/snapshot`
- `GET /api/v1/psyche/graph`
- `POST /api/v1/psyche/lock`
- `POST /api/v1/psyche/imprint`
- `POST /api/v1/psyche/recall`
- `POST /api/v1/psyche/replay`
- `POST /api/v1/psyche/compose`
- `POST /api/v1/psyche/beat` — five-organ Kawsay pulse. Occupancy MEASURED. Presence CONJECTURE. AGI CONJECTURE.
- `POST /api/v1/psyche/sense` — second-brain handles. SOFTWARE, HANDLES_ONLY, never LIVE.
- `POST /api/v1/psyche/graft` — imprint Yachay handles into Yuyay. Human Lock required.

## Kawsay

One beat fires Puriq → Yuyay → Tinku → Khipu → Lloqsi. Neighbor coupling uses
γ = α ≈ 0.138 on the organ pentagon. The order parameter R is MODELED, not a
proof of presence. Occupancy (ALLOW count / 5) is MEASURED. Phenomenal presence
and AGI stay CONJECTURE. Joules stay `null`.
